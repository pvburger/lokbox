import { StyleSheet } from 'react-native';

export const inputBox = StyleSheet.create({
  inputBox: {
    margin: 25,
    width: 350,
    height: 100,
    borderRadius: 25,
    borderColor: 'black',
    borderWidth: 4,
    backgroundColor: 'white',
    elevation: 5,
    fontSize: 30,
    padding: 25,
  },
});
